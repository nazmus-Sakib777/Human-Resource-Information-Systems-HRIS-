import { EmployeeInformation } from './employee-information';

describe('EmployeeInformation', () => {
  it('should create an instance', () => {
    expect(new EmployeeInformation()).toBeTruthy();
  });
});
